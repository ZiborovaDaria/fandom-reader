# Apply notes — fix-edge-chrome-scan-nav

## Manual checklist (phone)

1. **Light theme status bar** — Settings → Светлая: time/battery icons must be dark on cream.
2. **Reader insets** — open chapter: first line below status bar.
3. **Chrome** — tap text: opaque panel, ←/→ arrows, Назад → library; scrollbar visible only with chrome.
4. **Progress** — leave mid-chapter via Назад, reopen: same chapter/offset.
5. **Browse Back** — Catalog → Fandoms/Pairings/Tags → Назад works.
6. **Hide search** — Library → «Скрыть поиск» collapses fields; query kept.
7. **Downloads scan** — put `.fb2` in `Download`, tap Сканировать.
8. **Custom folder** — ⋮ → «Сканировать папку…» → pick Download → books import.

`adb` may be unavailable in CI agent; verify on device after `assembleDebug`.
