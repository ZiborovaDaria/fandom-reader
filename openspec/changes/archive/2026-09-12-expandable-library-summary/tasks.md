## 1. WorkList preview + expand

- [x] 1.1 В `WorkList` (`LibraryScreens.kt`) заменить `.take(220)` на константу свёрнутого превью `440`, показывать `summaryRu ?: summary`, и для длинных текстов добавить локальный expand state + кнопку «Ещё»/«Свернуть»; проверить визуально/в preview, что короткое summary без кнопки, длинное — с кнопкой
- [x] 1.2 Обеспечить, что клик по «Ещё»/«Свернуть» не вызывает `onOpenWork` (вложенный clickable или перенос clickable на headline); проверить Compose UI-тестом с флагом/счётчиком вызовов `onOpenWork`

## 2. Tests

- [x] 2.1 Расширить `WorkListComposeTest`: длинное summary (>440) — в свёрнутом виде виден превью-фрагмент и «Ещё», после клика виден полный текст и «Свернуть»; короткое summary — без «Ещё»; убедиться, что тесты проходят через `./gradlew :feature:library:test`
- [x] 2.2 Сохранить сценарий title+короткое summary (`showsTitleAndSummary`) зелёным после изменений

## 3. Verification

- [x] 3.1 Прогнать `./gradlew :feature:library:test` (или `./gradlew test`) — все связанные тесты зелёные
- [x] 3.2 Ручная проверка на полке «Фанфики»: длинная аннотация показывает больше текста; «Ещё» разворачивает inline; книга не открывается от кнопки
